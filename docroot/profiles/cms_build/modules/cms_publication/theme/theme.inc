<?php

/**
 * Default renderer for the instruments table inside the publication profile page
 * @param $variables
 * @return string
 */
function theme_table__publications_template_instrument($variables) {
    return theme_table($variables);
}
